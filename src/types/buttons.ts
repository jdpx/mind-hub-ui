import { Center, Left, Primary, Right, Secondary } from '../constants/buttons'

export type ButtonTypes = typeof Primary | typeof Secondary

export type Positions = typeof Right | typeof Left | typeof Center
